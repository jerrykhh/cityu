{
    "binary": 1,
    "filename": 1,
    "filesize": [10],
    "timeout": 2,
    "seed": 10000019,
    "description": "single file test, a small file of ${filesize} (${timeout} sec timeout)",
    "preparation": "cat ${filename} > /dev/null"
}